import styled from "styled-components";
import theme from "../layout/styles/Theme";
import {font} from "../layout/styles/Common";

export const SectionTitle = styled.h2`
  text-align: center;
  position: relative;
 ${font({weight: 700, fmax: 50, fmin: 36})};
  margin: 10px;
 margin-bottom: 40px;
 }
 `